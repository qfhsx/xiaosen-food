# 🍜 小森美食 - 服务器迁移指南

## 快速迁移步骤（5分钟完成）

### 1. 在新服务器创建项目目录

```bash
mkdir -p /opt/xiaosen-food
cd /opt/xiaosen-food
```

### 2. 上传项目文件

从本地电脑上传以下文件到新服务器的 `/opt/xiaosen-food/` 目录：

```
xiaosen-food/
├── docker-compose.yml      # Docker编排文件
├── init-db.sql             # 数据库初始化脚本
├── README-Docker.md        # 部署文档
├── backend/                # 后端代码
│   ├── Dockerfile
│   ├── go.mod
│   ├── go.sum
│   ├── cmd/
│   │   └── main.go
│   ├── internal/
│   │   ├── config/
│   │   ├── handler/
│   │   ├── middleware/
│   │   ├── model/
│   │   ├── repository/
│   │   └── service/
│   └── .env
├── frontend/               # 前端网站
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.ts
│   ├── index.html
│   └── src/
└── admin/                  # 管理后台
    ├── Dockerfile
    ├── nginx.conf
    ├── package.json
    ├── vite.config.ts
    ├── index.html
    └── src/
```

### 3. 一键启动

```bash
cd /opt/xiaosen-food
sudo docker-compose up -d
```

等待2-3分钟，服务启动完成。

### 4. 访问服务

| 服务 | 地址 |
|------|------|
| 🌲 前端网站 | http://服务器IP:8080 |
| 🖥️ 管理后台 | http://服务器IP:8081 |
| 🔧 后端API | http://服务器IP:8082 |

**管理后台账号**: admin / admin123

---

## 详细迁移方法

### 方法一：使用 SCP 上传（推荐）

**在本地电脑执行：**

```bash
# 打包项目（不包含node_modules）
cd ~/Desktop/work
tar czvf xiaosen-food.tar.gz \
  docker-compose.yml init-db.sql README-Docker.md \
  backend/ frontend/ admin/

# 上传到服务器
scp xiaosen-food.tar.gz root@你的服务器IP:/opt/
```

**在服务器执行：**

```bash
# 解压
cd /opt
tar xzvf xiaosen-food.tar.gz

# 启动
cd xiaosen-food
sudo docker-compose up -d
```

### 方法二：使用 Git 迁移

如果你使用 Git 管理代码：

```bash
# 在本地提交并推送
cd ~/Desktop/work/foodie-tech
git init
git add .
git commit -m "Initial commit"
git remote add origin 你的Git仓库地址
git push -u origin master

# 在服务器克隆并启动
cd /opt
git clone 你的Git仓库地址 xiaosen-food
cd xiaosen-food
sudo docker-compose up -d
```

### 方法三：使用 rsync 同步

```bash
# 本地执行
rsync -avz --exclude='node_modules' \
  ~/Desktop/work/foodie-tech/ \
  root@服务器IP:/opt/xiaosen-food/

# 然后SSH到服务器启动
ssh root@服务器IP "cd /opt/xiaosen-food && docker-compose up -d"
```

---

## 数据迁移（含数据库数据）

如果你想迁移**当前数据库里的数据**（菜品、用户等）：

### 1. 备份当前数据库

```bash
# 在当前服务器执行
sudo docker exec xiaosen_mysql mysqldump -uxiaosen -pxiaosen123 xiaosen_food > ~/xiaosen_backup.sql
```

### 2. 复制备份到新服务器

```bash
scp ~/xiaosen_backup.sql root@新服务器IP:/opt/
```

### 3. 在新服务器恢复数据

```bash
# 先按上面步骤启动项目
cd /opt/xiaosen-food
sudo docker-compose up -d mysql

# 等待MySQL启动完成（约30秒）
sleep 30

# 恢复数据
sudo docker exec -i xiaosen_mysql mysql -uxiaosen -pxiaosen123 xiaosen_food < /opt/xiaosen_backup.sql

# 重启后端
sudo docker-compose restart backend
```

---

## 完整打包（一键部署包）

如果你想创建一个**包含所有数据**的完整部署包：

```bash
# 在当前服务器执行

# 1. 导出数据库
cd ~/Desktop/work
sudo docker exec xiaosen_mysql mysqldump -uxiaosen -pxiaosen123 xiaosen_food > foodie-tech/init-db-with-data.sql

# 2. 修改docker-compose使用带数据的SQL
sed -i 's/init-db.sql/init-db-with-data.sql/' foodie-tech/docker-compose.yml

# 3. 打包整个项目
tar czvf xiaosen-food-complete.tar.gz \
  --exclude='*/node_modules' \
  --exclude='*/.git' \
  foodie-tech/

# 4. 还原修改
sed -i 's/init-db-with-data.sql/init-db.sql/' foodie-tech/docker-compose.yml

echo "✅ 完整部署包已生成: xiaosen-food-complete.tar.gz"
```

然后上传到新服务器解压启动即可。

---

## 生产环境配置

### 1. 修改默认密码

编辑 `docker-compose.yml`：

```yaml
mysql:
  environment:
    MYSQL_ROOT_PASSWORD: 你的强密码
    MYSQL_PASSWORD: 你的强密码

backend:
  environment:
    DB_PASSWORD: 你的强密码
    JWT_SECRET: 你的随机密钥
```

### 2. 配置域名和HTTPS

使用 Nginx 反向代理：

```bash
sudo apt install nginx certbot python3-certbot-nginx

# 配置 Nginx
sudo tee /etc/nginx/sites-available/xiaosen << 'EOF'
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

server {
    listen 80;
    server_name admin.your-domain.com;
    
    location / {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

sudo ln -s /etc/nginx/sites-available/xiaosen /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 申请SSL证书
sudo certbot --nginx -d your-domain.com -d admin.your-domain.com
```

### 3. 设置自动备份

```bash
# 创建备份脚本
sudo tee /opt/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/xiaosen"
mkdir -p $BACKUP_DIR

# 备份数据库
docker exec xiaosen_mysql mysqldump -uxiaosen -pxiaosen123 xiaosen_food > $BACKUP_DIR/backup_$DATE.sql

# 保留最近30天的备份
find $BACKUP_DIR -name "backup_*.sql" -mtime +30 -delete

echo "[$DATE] 备份完成"
EOF

sudo chmod +x /opt/backup.sh

# 添加到定时任务（每天凌晨3点备份）
echo "0 3 * * * /opt/backup.sh >> /var/log/xiaosen-backup.log 2>&1" | sudo crontab -
```

---

## 常见问题

### 1. 端口被占用

修改 `docker-compose.yml` 中的端口映射：

```yaml
frontend:
  ports:
    - "9090:80"  # 改为9090端口

admin:
  ports:
    - "9091:80"  # 改为9091端口
```

### 2. 数据库连接失败

检查MySQL是否完全启动：

```bash
sudo docker-compose logs mysql
sudo docker-compose restart backend
```

### 3. 图片加载慢/失败

使用本地图片或CDN：

```bash
# 下载图片到本地
mkdir -p /opt/xiaosen-food/images
cd /opt/xiaosen-food/images
wget https://images.unsplash.com/photo-xxx -O hongshaorou.jpg
# ...下载其他图片

# 修改数据库图片路径
sudo docker exec xiaosen_mysql mysql -uxiaosen -pxiaosen123 -e "
USE xiaosen_food;
UPDATE foods SET image='/images/hongshaorou.jpg' WHERE id=1;
"

# 挂载图片目录到前端容器
# 在docker-compose.yml frontend服务中添加：
volumes:
  - ./images:/usr/share/nginx/html/images
```

---

## 总结

✅ **迁移非常简单**：
1. 上传项目文件
2. 运行 `docker-compose up -d`
3. 完成！

✅ **包含数据迁移**：
- 数据库结构和数据都在 `init-db.sql` 中
- 运行时自动初始化

✅ **生产环境就绪**：
- 支持域名和HTTPS
- 支持自动备份
- 支持横向扩展

有任何问题随时问我！
